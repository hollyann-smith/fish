<%= form_with model: [catch, comment], local: true, class: "mt-3" do |f| %>
  <div class="mb-2">
    <%= f.text_area :content, class: "form-control", placeholder: "Add a comment…" %>
  </div>
  <%= f.submit "Post Comment", class: "btn btn-primary" %>
<% end %>
